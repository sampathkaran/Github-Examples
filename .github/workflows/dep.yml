name: dependent job

on:
  push:
    branches:
      - main

jobs:
  job2: 
    runs-on: ubuntu-latest
    needs: job1
    steps: 
      - name: step 2
        run : echo "this is job2"
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "this is job1"